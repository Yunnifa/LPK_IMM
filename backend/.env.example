# ===========================================
# LPK IMM Backend Environment Configuration
# ===========================================

# Database Connection
# Railway PostgreSQL Format:
DATABASE_URL=postgresql://postgres:password@host:5432/railway

# JWT Secret Key (REQUIRED - generate a secure random string)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-key-min-32-characters

# Server Port (Railway sets this automatically)
PORT=3000

# Allowed Origins for CORS (comma-separated)
# Example: https://your-frontend.up.railway.app,https://another-domain.com
ALLOWED_ORIGINS=https://your-frontend.up.railway.app

# Telegram Bot Token (optional, for notifications)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
