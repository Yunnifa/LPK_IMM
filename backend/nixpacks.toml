# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci --include=dev || npm install"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "npm run start"
